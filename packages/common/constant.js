const path = require('path')

const PATHS = {
    rootNodeModules: path.join(__dirname, '../../node_modules/'),
    rootDir: path.join(__dirname, '../../'),
}
const COLORS = {
    green: '#07c160',
}

module.exports = {
    PATHS,
    COLORS,
}
